import { WorkspacePage } from "@/components/workspace/WorkspacePage";

export default function SchoolPage() {
  return (
    <WorkspacePage
      eyebrow="School"
      title="School filter"
      description="School obligations remain first-class agenda items while using the same underlying data model as every other workspace surface."
    />
  );
}
