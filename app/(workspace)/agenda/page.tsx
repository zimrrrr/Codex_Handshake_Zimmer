import { WorkspacePage } from "@/components/workspace/WorkspacePage";

export default function AgendaPage() {
  return (
    <WorkspacePage
      eyebrow="Agenda"
      title="Your next seven days"
      description="The agenda is the operational center of the product: one place for synced events, app-local tasks, and assistant-relevant preparation."
    />
  );
}
