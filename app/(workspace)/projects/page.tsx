import { WorkspacePage } from "@/components/workspace/WorkspacePage";

export default function ProjectsPage() {
  return (
    <WorkspacePage
      eyebrow="Projects"
      title="Projects filter"
      description="Projects is a focused agenda slice, not a separate model. This view will filter the shared agenda by category."
    />
  );
}
