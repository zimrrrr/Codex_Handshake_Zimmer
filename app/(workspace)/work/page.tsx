import { WorkspacePage } from "@/components/workspace/WorkspacePage";

export default function WorkPage() {
  return (
    <WorkspacePage
      eyebrow="Work"
      title="Work filter"
      description="Work captures internships, shifts, recruiting steps, and any career-oriented tasks without creating a second planning system."
    />
  );
}
